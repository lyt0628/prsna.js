module.exports = {
    printWidth: 100, // max char in a single line
    tabWidth: 2, // tab as 2 space
    useTabs: false, // convert Tab as space
    semi: true, // need ; in end of statement
    singleQuote: true, // can use ''
    quoteProps: 'as-needed', // use '' in field only when must
    trailingComma: 'all', // need , as end of line
    bracketSpacing:true, // ned left and right space in [ ... ]
    arrowParens: 'always', // arrow Fn need () even 0 param
    rangeStart: 0
}